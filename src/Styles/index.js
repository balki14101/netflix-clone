// library imports
import {StyleSheet, Dimensions} from 'react-native';

const globalStyles = StyleSheet.create({
  screenCenter: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

const colors = {
  blue: 'blue',
  bgBlack: 'rgba(0,0,0,0.7)',
};

const iconSize = {
  small: 'small',
  large: 'large',
};

// const screenWidth = Dimensi
/**
 * Movie card
 */

// const movieCard = {
//   width:
// }

// default export
export default globalStyles;

//named export
export {colors, iconSize};
